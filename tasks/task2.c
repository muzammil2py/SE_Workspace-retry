#include<stdio.h>
#include<conio.h>
void main()
{
    int a=5,b=5;
    int add=a+b;
    int sub=a-b;
    int mul=a*b;
    int div=a/b;

    printf("\n the addition is=%d",add);

    printf("\n the substaction is=%d",sub);

    printf("\n the multiplication is=%d",mul);

    printf("\n the division is=%d",div);

    getch();
    
}